
name: Build and Deploy Auth Service to EKS

on:
  push:
    branches: [main]
    paths:
      - 'auth_service/**'  # Only trigger if auth_service code changed
      - 'charts/auth-service/**'
      - '.github/workflows/auth-service-deploy.yml'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      # Step 1: Get the code
      - name: Checkout code
        uses: actions/checkout@v3
      
      # Step 2: Configure AWS credentials
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: eu-central-1
      
      # Step 3: Login to ECR
      - name: Login to Amazon ECR
        run: |
          aws ecr get-login-password --region eu-central-1 | \
          docker login --username AWS --password-stdin 114851843413.dkr.ecr.eu-central-1.amazonaws.com

      # Step 4: Set up Go
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25.6'

      # Step 5: Run tests
      - name: Run tests
        run: |
          cd auth_service
          go test ./... -v -race -coverprofile=coverage.out
          go tool cover -func=coverage.out

      # Step 6: Build Docker image
      - name: Build Docker image
        run: |
          docker build -f auth_service/Dockerfile \
            -t 114851843413.dkr.ecr.eu-central-1.amazonaws.com/go-microservice/auth-service:${{ github.sha }} .
          docker tag \
            114851843413.dkr.ecr.eu-central-1.amazonaws.com/go-microservice/auth-service:${{ github.sha }} \
            114851843413.dkr.ecr.eu-central-1.amazonaws.com/go-microservice/auth-service:latest
      
      # Step 7: Scan Docker image with Trivy
      - name: Scan Docker image with Trivy
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: '114851843413.dkr.ecr.eu-central-1.amazonaws.com/go-microservice/auth-service:${{ github.sha }}'
          format: 'table'
          exit-code: '1'
          ignore-unfixed: true
          vuln-type: 'os,library'
          severity: 'CRITICAL,HIGH'

      # Step 8: Push to ECR
      - name: Push to Amazon ECR
        run: |
          docker push 114851843413.dkr.ecr.eu-central-1.amazonaws.com/go-microservice/auth-service:${{ github.sha }}
          docker push 114851843413.dkr.ecr.eu-central-1.amazonaws.com/go-microservice/auth-service:latest
      
      # Step 9: Update kubeconfig
      - name: Update kubeconfig
        run: |
          aws eks update-kubeconfig \
            --region eu-central-1 \
            --name go-microservices
      
      # Step 10: Reconstruct secrets file from GitHub Actions Secrets
      # The secrets/ directory is gitignored - we build it on the fly here
      - name: Create secrets file
        env:
          MONGO_URI: ${{ secrets.AUTH_SERVICE_MONGO_URI }}
          JWT_SECRET: ${{ secrets.AUTH_SERVICE_JWT_SECRET }}
          RABBITMQ_URI: ${{ secrets.AUTH_SERVICE_RABBITMQ_URI }}
          REDIS_ADDR: ${{ secrets.AUTH_SERVICE_REDIS_ADDR }}
          REDIS_TLS: ${{ secrets.AUTH_SERVICE_REDIS_TLS }}
        run: |
          mkdir -p secrets
          cat <<EOF > secrets/auth-service.values.prod.yaml
          secrets:
            mongoUri: "${MONGO_URI}"
            jwtSecret: "${JWT_SECRET}"
            rabbitmqUri: "${RABBITMQ_URI}"
            redisAddr: "${REDIS_ADDR}"
            redisTls: "${REDIS_TLS}"
            userServiceAddr: "user-service.prod.svc.cluster.local:50051"
          EOF

      # Step 11: Ensure namespace exists
      - name: Create namespace if not exists
        run: |
          kubectl get namespace prod || kubectl create namespace prod

      # Step 12: Deploy with Helm
      - name: Deploy with Helm
        run: |
          helm upgrade --install auth-service ./charts/auth-service \
            -f charts/auth-service/values.yaml \
            -f secrets/auth-service.values.prod.yaml \
            --set image.tag=${{ github.sha }} \
            --namespace prod \
            --atomic \
            --timeout 5m
      
      # Step 13: Verify deployment
      - name: Verify deployment
        run: |
          kubectl rollout status deployment/auth-service -n prod --timeout=5m
          kubectl get pods -n prod -l app.kubernetes.io/instance=auth-service